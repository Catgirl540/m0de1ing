---
description: 一键建赛：生成标准项目目录与全套模板
---
用户输入（项目名，如 2026C_管道运输）：

$ARGUMENTS

执行要求：
1. 运行 `python tools/new_project.py <项目名>`（目录已存在且用户确认时加 --force）；
2. 展示生成的目录结构与「进度看板.md」要点；
3. 引导用户进入 G1 门禁：把题目原文投给 `/审题`，产出 `01_审题/题目分析.md`。
