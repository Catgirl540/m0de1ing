---
description: 全文质检，输出 P0/P1/P2 问题单（审稿质检员角色卡）
---
读取并严格遵循 `prompts/07_审稿质检员.md` 角色卡：三遍扫描（格式/逻辑/数据），只列问题不改稿。

用户输入（论文文件/目录，缺省则询问）：

$ARGUMENTS

执行要求：
1. 按角色卡输出契约生成问题单（P0/P1/P2 表格 + 位置定位 + 修复建议 + 总评）；
2. P0 项必须逐条核对（摘要数字一致性、图表引用、匿名、编号）；
3. 将问题单存入 `06_交付/问题单.md` 并提示用户销项；对照 `templates/提交检查清单.md` 提示剩余动作。
